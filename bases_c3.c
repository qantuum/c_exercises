#include <stdio.h>
void main()
{
	int z = 10;
	int a = 22;
	printf("la variable z vaut : %d et a vaut : %d",z,a);
}

// %d = entier signé (char, short, int) || d ça veut dire décimal
// %u = entier non-signé (char, short, int)
// %ld = entier signé long
// %lu = entier non-signé long
// %x = écriture sous forme hexadécimale !!! 
// %c = sous forme d'un caractère
// %s = chaîne de caractère
// %f = float
// %lf = double

// plus d'info : man printf sur google ou man x printf en shell (terminal) -- x=2 ou 3.. des fois des fonctions shell ont le même nom que dans d'autres langages...

// shell code stack overflow